{
  "name": "cypressautomation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cypress run --env allure=true",
    "test:report": "npx allure generate allure-results --clean -o allure-reports",
    "open": "cypress open"
  },
  "author": "Hasna",
  "license": "ISC",
  "dependencies": {
    "cypress-parallel": "^0.13.0"
  },
  "devDependencies": {
    "@cypress/xpath": "^2.0.3",
    "@shelex/cypress-allure-plugin": "^2.40.0",
    "cypress": "^14.3.2",
    "cypress-mochawesome-reporter": "^3.5.0",
    "npm-run-all": "^4.1.5",
    "start-server-and-test": "^2.0.0"
  }
}
